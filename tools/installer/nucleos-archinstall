#!/usr/bin/env python3
"""Launcher for the CortexOS Archinstall-derived adapter."""
from pathlib import Path
import runpy

entry = Path(__file__).resolve().parents[1] / "archinstall" / "nucleos.py"
runpy.run_path(str(entry), run_name="__main__")
