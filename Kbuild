# CortexOS source inventory, kept close to Linux-style subsystem layout.
# The active build is currently driven by Makefile/KERNEL_SRC_DIRS.
obj-y += kernel/core/
obj-y += drivers/
obj-y += block/
obj-y += fs/
obj-y += ipc/
obj-y += mm/
obj-y += net/
obj-y += virt/
